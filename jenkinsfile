pipeline {
agent any

stages{
stage('stahe1'){
steps{
  echo 'stage1: Checkout SCM'  
   checkout scm
    }
}
stage('stage2: Maven Build'){
 steps{
echo 'stage2: Maven Build'
sh 'mvn clean package'
}
}
  }
}
